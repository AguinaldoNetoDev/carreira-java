package desafios.sectionOne.exercises;

import java.util.Scanner;

public class ControlFluxAndConditionals {

    public static void main(String[] args) {

        Scanner scanner = new Scanner(System.in);

        /*
         * 1- Create a program that prompts the user to enter a number.
         * If the number is positive, display "Positive number", otherwise, display "Negative number".
        *
        System.out.println("Enter with a number:");
        int enteredNumber = scanner.nextInt();

        System.out.printf("The entered number was: %2d\n", enteredNumber);
        if (enteredNumber <= 0) {
            System.out.println("Negative number!");
            return;
        }

        System.out.println("Positive number!");


        /*
         * 2- Ask the user to enter two integers.
         * Compare the numbers and print a message indicating whether they are equal, different, the first is greater, or the second is greater.
        *
        System.out.println("Enter with two numbers:");
        System.out.println("First number: ");
        int firstNumber = scanner.nextInt();

        System.out.println("Second number: ");
        int secondNumber = scanner.nextInt();

        if (firstNumber < secondNumber) {
            System.out.println("The second number is greater than the first.");
        } else if (firstNumber > secondNumber) {
            System.out.println("The first number is greater than the second.");
        } else {
            System.out.println("The two numbers are the same.");
        }
*/

        /*
        * 3-Create a menu that offers the user two options:
        * 3.1. Calculate the area of square
        * 3.2. Calculate the area of circle.
        * Prompt the user to choose and perform the area calculation based on the selected option.
        *

        System.out.println("Enter 1 to calculate the area of square or 2 to calculate the area of circle:");
        int enteredNumber = scanner.nextInt();

        if (enteredNumber == 1) {
            System.out.println("Enter a value and i'll calculating the area of square:");
            int squareAreaValue = scanner.nextInt();

            int squareArea = squareAreaValue * squareAreaValue;
            System.out.printf("The value of square area is: %d", squareArea);


        } else if(enteredNumber == 2) {
            System.out.println("Enter a value and i'll calculating the area of circle:");
            double circleAreaValue = scanner.nextDouble();
            double pi = 3.14159;

            double circleArea = circleAreaValue * circleAreaValue * pi;
            System.out.printf("The value of square area is: %2f", circleArea);
        } else {
            System.out.println("Invalid number. Enter again with 1 or 2.");
        }
*/


        /*
        * 4-
        * */

        /*
        * 5-
        * */

        /*
        * 6-
        * */
    }
}
